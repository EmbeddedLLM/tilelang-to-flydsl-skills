"""Manifold HyperConnection kernels are accessed through the modeling layer
(fly_tile_kernels.modeling.mhc.ops.*).  The kernel modules below are kept for
parity with the upstream layout but are not re-exported from this package."""
